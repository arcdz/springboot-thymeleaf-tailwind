# Spring Boot with Thymeleaf and TailwdinCSS

## config tailwindcss
Init:
```terminal
npm init -y
npm i -D tailwindcss
npx tailwindcss init
```

Run: 
```terminal
npx tailwindcss -i ./src/main/resources/static/input.css -o ./src/main/resources/static/output.css --watch
```